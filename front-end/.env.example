# Base URL of the Ingredient Checker API.
#
# Vite inlines VITE_* variables at BUILD time, so this must be set before
# `npm run build` — setting it only on the web host has no effect.
# Leave unset for local development to fall back to http://localhost:5000.
VITE_BACKEND_URL=https://your-backend.example.com
